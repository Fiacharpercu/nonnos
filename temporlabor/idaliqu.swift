let english = TypesettingLanguage(name: "English", abbreviation: "EN")
let french = TypesettingLanguage(name: "French", abbreviation: "FR")

// Checking equality
if english == french {
    print("The languages are the same.")
} else {
    print("The languages are different.")
}
